# Install with:  .venv/bin/pip install -r requirements.txt
# Requires Python >= 3.11 (dataclasses + modern asyncio)
httpx>=0.27.0
pyyaml>=6.0
requests>=2.31.0      # used by export_cee_visuals.py for visual export
